# Generated by stubbs:add-option. Do not edit, if using stubbs.
# Created: Mon Dec  3 14:03:26 PST 2012
#
#/ usage: python-setuptools:build [ --format <rpm>]  --version <>  --directory <> [ --url <http://pypi.python.org/packages/source>]  --name <> [ --release <1>] 

# _rerun_options_parse_ - Parse the command arguments and set option variables.
#
#     rerun_options_parse "$@"
#
# Arguments:
#
# * the command options and their arguments
#
# Notes:
# 
# * Sets shell variables for any parsed options.
# * The "-?" help argument prints command usage and will exit 2.
# * Return 0 for successful option parse.
#
rerun_options_parse() {
    
    while [ "$#" -gt 0 ]; do
        OPT="$1"
        case "$OPT" in
            --format) rerun_option_check $# ; FORMAT=$2 ; shift ;;
            --version) rerun_option_check $# ; VERSION=$2 ; shift ;;
            --directory) rerun_option_check $# ; DIRECTORY=$2 ; shift ;;
            --url) rerun_option_check $# ; URL=$2 ; shift ;;
            --name) rerun_option_check $# ; NAME=$2 ; shift ;;
            --release) rerun_option_check $# ; RELEASE=$2 ; shift ;;
            # help option
            -?)
                rerun_option_usage
                exit 2
                ;;
            # end of options, just arguments left
            *)
              break
        esac
        shift
    done

    # Set defaultable options.
    [ -z "$FORMAT" ] && FORMAT="rpm"
    [ -z "$URL" ] && URL="http://pypi.python.org/packages/source"
    [ -z "$RELEASE" ] && RELEASE="1"
    # Check required options are set
    [ -z "$VERSION" ] && { echo >&2 "missing required option: --version" ; return 2 ; }
    [ -z "$DIRECTORY" ] && { echo >&2 "missing required option: --directory" ; return 2 ; }
    [ -z "$NAME" ] && { echo >&2 "missing required option: --name" ; return 2 ; }
    # If option variables are declared exportable, export them.

    #
    return 0
}


# Initialize the options variables to null.
FORMAT=
VERSION=
DIRECTORY=
URL=
NAME=
RELEASE=


